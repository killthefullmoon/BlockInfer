Metadata-Version: 2.4
Name: blockinfer
Version: 0.1.0
Summary: a lightweight inference engine tailored for SDAR model (block diffusion) built from nanovllm
Author: Hui Shen
License-Expression: MIT
Requires-Python: <3.13,>=3.10
Description-Content-Type: text/markdown
License-File: LICENSE
Requires-Dist: torch>=2.4.0
Requires-Dist: triton>=3.0.0
Requires-Dist: transformers>=4.51.0
Requires-Dist: liger-kernel
Requires-Dist: flashinfer-python
Requires-Dist: xxhash
Dynamic: license-file

# BlockInfer

## Installation
### Environment Setup

```
transformers>=4.52.4
flash-attn
```

For Local Inference:

```bash
pip install flash-attn --no-build-isolation
git clone https://github.com/Labman42/BlockInfer.git
cd BlockInfer
pip install .
```
For RL training usage (support DP and TP, managed by accelerate from huggingface):

```bash
pip install flash-attn --no-build-isolation
git clone https://github.com/Labman42/BlockInfer.git
cd BlockInfer
git checkout accelerate
pip install .
```

## Quick Start

```bash
python example.py
```

See `example.py` for usage. The API mirrors vLLM's interface with some differences in the `LLM.generate` method.

